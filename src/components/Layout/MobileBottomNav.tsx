import React from 'react';

// This component is no longer needed for the simplified mobile interface
// Keeping the file for potential future use but exporting an empty component

export const MobileBottomNav = () => {
  return null;
};
